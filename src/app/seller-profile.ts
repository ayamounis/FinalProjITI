export interface SellerProfile {
  sellerProfileId: number;
  storeName: string;
  description: string;
  address: string;
  phoneNumber: string;
  isVerified: boolean;
  createdAt: string;
  userId: string;
}